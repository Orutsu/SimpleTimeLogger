import { StyleSheet } from 'react-native';
import { COLORS } from '../../styles';

export default StyleSheet.create({
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    color: COLORS.BLACK,
  },
  label: {
    fontSize: 18,
    color: COLORS.BLACK,
  },
  valueText: {
    fontSize: 16,
    color: COLORS.BLACK,
  },
});
