import { StyleSheet } from 'react-native';
import { COLORS } from '../../styles';

export default StyleSheet.create({
  label: {
    fontSize: 18,
    color: COLORS.BLACK,
  },
  valueText: {
    fontSize: 16,
    color: COLORS.BLACK,
  },
});
